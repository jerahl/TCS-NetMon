{
  "name": "netmon-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "TCS NetMon React UI (esbuild → static, served by FastAPI).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs && node render-check.mjs",
    "watch": "node build.mjs --watch",
    "render-check": "node render-check.mjs"
  },
  "dependencies": {
    "@xyflow/react": "12.3.6",
    "leaflet": "1.9.4",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.24.0"
  }
}
